import React from 'react';

const PremiumFooterNote: React.FC = () => {
    return (
        <div className="text-center text-sm text-gray-400 mt-8">
            <p>* The number of sites and servers allowed on a single account is limited to 5. All must be unique and placed inside proper gaming categories.</p>
            <p>** Maximum upload size for image file is 1 MB. If your banner exceeds this size, you need to contact us and we will manually upload.</p>
        </div>
    );
};

export default PremiumFooterNote;
